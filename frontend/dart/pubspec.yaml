name: chromium_history_extension
description: Dart bridge layer for Chromium History Chat Extension
version: 0.1.9
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  js: ^0.7.1
  http: ^1.1.0

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.0
  lints: ^3.0.0

# Dart compile configuration
# Use: dart compile js -o output.js input.dart
